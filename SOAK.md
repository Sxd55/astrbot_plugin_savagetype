# Savage Type 实机测试流程（v5.5.0）

本轮新增：**场景化预设（Presets）与协同避让（Coexistence）**。
提供 daily / frugal / assistant / rpg 4 套开箱即用场景优化；与 savagereply 自动握手避让。
只列**本轮新增 + 审计修复 + 上一版 + 历史未确认项**。

## 0. 升级

1. 覆盖目录 → 重载插件；`/stype status` 第一行应为 `v5.5.0`。
2. 升级会自动备份一次（诊断页可见）；老库首次启动会回填实体链接与约定分槽，慢几秒正常。
3. 检索行应为 `bm25 开(jieba)`；显示 `builtin` 表示 jieba 没装上（功能仍可用，只是字符切分）。

## 0.3 跨会话一致性验证

（v4.6.0 追加）窗口全流（C 层）验证：

（v4.7.0 追加）免@主动接话验证：

（v4.8.0 追加）指派发言验证：

（v4.9.0 追加）谁对谁说 / 空@提醒 / 防抖验证：

（v5.0.0 追加）免@接话 v2 验证：

（v5.1.0 追加）疑问句命中验证：

1. 面板写一条备注类事实：属性 `note`、值填短话题词（如「闺蜜」）、内容写完整叙述（如「她俩大一社团认识，闺蜜叫小美」）；
2. 聊天里问「你闺蜜是谁」→ 确认 Bot 直接答得出来（不再需要"查一下"）；`/stype diagnostics` 的 `inject` 里该条不应再是 `query_mentioned`；
3. 聊天里陈述「我闺蜜是小美」→ 确认仍然不会重复注入（防重复行为未破坏）。

（v5.2.0 追加）关键词必回验证：

1. 配置 `reply_gate_enabled=true`、`reply_gate_keywords_force=true`、`reply_gate_keywords=问一下`，冷却/硬间隔先设 0；
2. 群里发「问一下这个怎么弄」→ 确认 Bot 必回；`/stype diagnostics` 的 `reply_gate` 里 `reason=keyword_force`、`keyword=keyword:问一下`；
3. 发「@某人 问一下他」→ 确认仍然回（越过话轮）；再把 `reply_gate_keywords_force` 关掉重发 → 变成不回（`reason=turn_not_open`）。

（v5.3.0 追加）审查修补验证：

1. 无人应答：群里发个问句后 20 秒谁都别说话 → 确认 Bot 接「没人答我来」（`reply_gate_delayed.reason=unanswered`）；有人先回则不接；
2. 防抖换人：A 发「在吗」、1 秒内 B 发「我来说」→ 两条都不丢（A 的合并发出，B 的另起合并）；
3. 关键词 `问一下` + 开必回：发「@某人 问一下他」→ 回；关必回重发 → 不回；
4. `/stype flow` 非管理员/主人执行 → 提示无权限；管理员执行正常；
5. ChatUI 不用时可关 `webchat_is_owner`（默认开保持现状）。


1. 配置 `reply_gate_enabled=true`、`reply_gate_mode=judge`、阈值 0.6、`reply_gate_min_interval_seconds=0`；
2. 群里发一条开放话轮的消息（不@任何人），确认 Bot 按判定接话，`/stype diagnostics` 里 `reply_gate` 的 `reason=judge_pass`；
3. 发一条 `@某人 你看下` 的消息，确认 Bot **不接**（`reason=turn_not_open`）；
4. 在消息里直接叫 Bot 的名字（不@），确认必接（`reason=name_hit`）；
5. 发一条问句后不管它，等 `reply_gate_unanswered_seconds` 秒，确认 Bot 主动接（`reply_gate_delayed.reason=unanswered`）；发问句后让其他人先回，确认 Bot 不接（`reason=answered`）；
6. 配置 `reply_gate_quiet_hours` 覆盖当前时间，确认不接话（`reason=quiet_hours`）。


1. 群里发一条带 @ 的消息（如「@甲 看下这个」），私聊发 `/stype flow`，确认窗口全流里显示「谁 → 甲」；
2. 群里先与 bot 正常对话一次，随后只 @ bot 不发正文，确认回复能接上刚才的话题（日志 `blank_mention` 有记录）；
3. 私聊连发三条短消息（如「在吗」「那个」「帮我看下」），开启 `debounce_enabled` 后确认 bot 只回一次、且内容对得上三条合并；
4. 发一条带句号的长消息，确认不等待、立即回复；
5. `@bot` 提问，确认不被防抖挂起（`debounce_skip_wake` 默认开）。


1. 插件配置打开 `speak_enabled`；先在目标群里说一句话（让插件记录该群），私聊发 `/stype groups` 确认能看到群与编号；
2. 私聊发 `/stype default <群号或序号>`，确认回执「默认群已设为 X」；
3. 私聊发「去群里说：晚上八点开黑」，确认目标群收到这句话，且私聊收到回执；
4. 私聊发「去 1 群说：测试序号」，确认按序号发到对应群；发不存在的群号确认有提示；
5. 连发 6 条（默认每分钟 5 条上限），确认第 6 条被限流拦截；
6. 用非主人账号私聊同样的句子，确认只回「只有主人能让我去群里说话」且不发群。


1. 插件配置里打开 `reply_gate_enabled`，模式选 `probability`、概率设 1.0、冷却设 0；
2. 在群里发一条普通消息（不 @ 机器人），确认 bot 主动回复；
3. 连续发两条，确认第二条被冷却/每日上限拦截（`/stype diagnostics` 的 `reply_gate` 能看到 reason）；
4. 模式改 `memory`，发一条跟已存记忆相关的话（如「美式咖啡还有吗」），确认接话；再发「？？？」确认不接；
5. 模式改 `keyword`，填「在吗」，确认只有含该词的消息才接话；
6. 确认 AstrBot 内置「主动回复」已关闭，避免双重接话。


1. 私聊里先发一句含关键词的话（如「群里什么情况」），确认 bot 能复述群里最近的具体发言与说话人；
2. 发 `/stype flow`，确认能直接看到窗口全流块本身（窗口数 / 条数 / 字符）；
3. 群聊里发一句含「私聊」关键词的话，确认群里也能引用你私聊里刚说的话（双向全通）；
4. 把某个用户 ID 填进 `window_flow_exclude_private_users`，确认其私聊内容不再出现在群聊注入里；
5. 把 `window_flow_always` 打开，确认不带关键词的消息每次也注入（提示词长度明显变大）；关闭后恢复。
（本轮重点）

1. **画像一致**：私聊里发 `我叫XX，喜欢喝美式` → 等整理完成 → `/stype profile`。
   期望：卡片含 `称呼：XX`、`偏好：美式`；换到群会话再查 → **同一张卡**（称呼/偏好不随场景变）。
2. **衔接（群→私）**：群里用同一账号发 `我周末要加班` → 立刻切私聊发 `在吗`。
   期望：回应自然延续，不出现「周末有空吗」这类矛盾；`/stype cross` 能看到
   `- …… 在群里说过：我周末要加班`。
3. **方向闸门**：私聊说 `我准备给朋友一个惊喜` → 到群里问 `刚才说什么了`。
   期望：群里**看不到**该内容（私→群默认关），`/stype cross` 显示「因方向被跳过 ≥1」；
   打开 `cross_window_private_to_group` 后才应出现。
4. **人格隔离**：多个人格时，A 人格会话说的话不应出现在 B 人格的衔接块里。
5. **关掉即无**：关掉 `cross_window_enabled` / `profile_inject_enabled` 后，
   注入包里不应再有 `【画像】` 与 `【衔接`。

## 0.4 捕获门验证（本轮重点）

按顺序发这几句，每发一句跟一次 `/stype extract`，看返回里的 `candidates` 和 `written`：

```text
我平时喜欢喝气泡水
我最近迷上了爬山
我其实不喜欢猫了
我家的猫叫团子
我不太喜欢甜食
```

期望：每句都能进候选（`candidates` ≥ 1），`/stype search 气泡水`、`/stype search 爬山` 能查到；
「我最近迷上了爬山」记成 likes 而不是 3 天过期的 status。
反例（应保持不进候选）：`我可爱吗`、`我朋友叫小明`、`我叫什么名字`。

## 0.5 模型调用策略验证（本轮重点）

1. **用量可见**：面板「记忆库」页顶部应有一行
   `今日模型用量 N tokens（硬限 不限 / 软限 不限）· 缩写 X / 审核 Y …`；
   `/stype status` 有对应一行。聊几句让插件跑一次整理后数字应增长。
2. **分档生效**：设置 → 「模型档位与 Token 预算」，精准档选强模型、快速档选便宜模型 → 保存 →
   手动 `/stype extract`。日志/面板应显示缩写走精准档、审核走快速档。
   若某任务之前单独配过模型，该任务仍用显式配置（显式优先）。
3. **硬限额**：`daily_token_limit` 设成一个很小的值（例如 1）→ `/stype extract`。
   期望：返回里 `reason=budget`；`/stype status` 显示「未总结」条数不变（**消息没有被丢掉**，
   额度恢复后会自动重试）；面板用量行出现「预算跳过 N 次」。
4. **软限额**：`daily_token_limit` 改回 0，`soft_token_limit` 设很小 →
   再 `/stype extract`：缩写/审核照常，表达学习与向量回填被跳过（用量行能看到跳过计数）。
5. **备用模型**：配好「备用模型」后，把 `single_call_token_cap` 设成 5 → 跑一次抽取。
   期望：改用备用模型完成，不中断。
6. 验证完把三个限额改回 0（不限）。
7. 拒答重试不容易手工造；若你有会拒答的模型，把它设为精准档，观察日志
   `llm refusal task=normalize, retrying with fallback`，且不把拒答文本当整理结果。

## 1. 本轮新增（v4.3.0：实体链接 + 时序查询）

1. **实体链接**：聊到「小明」并让插件记下一条事实 → 面板事实卡点「相关」，应列出 `person/keyword` 实体；
   在群里问「小明最近怎么样」→ 和小明相关的事实/事件应被排到前面（显微镜里能看到命中）。
2. **改名跟随**：把某人的昵称在档案里改掉 → 他的事实卡「相关」里的人名实体跟着变。
3. **时序查询（模糊）**：说「我以前喜欢喝什么」→ 注入显微镜 route 应为 `history`；包里出现 `【当时】` 块，
   被覆盖的旧说法带「现在：…」对照；不能把旧说法当现状回答。
4. **时序查询（具体时间）**：`/stype history 去年12月` 或问「2025年3月我干了什么」→ 只带那段时间的事实和事件。
5. **隐私照旧**：私聊说的旧事，在群里问「他以前…」不应出现在 `【当时】` 里（默认 strict）。
6. **面板**：事实卡显示「有效期」（被覆盖/归档的旧条）；设置页「事件记忆与隐私」新增实体/时序 4 项。
7. **导出导入**：JSONL 导入后实体链接自动重建（不需要额外字段）。

## 2. 审计修复验证（本轮必测）

1. **档案卡补漏（高危）**：让某人私聊说一个群里没说过的偏好（如「我养了两只猫」）→ 群里问「介绍一下他 / 他养了什么」→
   回复和注入包里都不能出现猫；`/stype dossier 他` 在群里看不到这条，私聊里能看到。
2. **改口成事件**：只发一句「我改口了，不喝美式了」→ `/stype extract` → 事件页多一条改口事件（不只是事实被覆盖）。
3. **整年问法**：问「2026年发生了什么」→ 显微镜 route 应为 `history`，带出当年事件。
4. **强制立即整理**：刚聊完一段（不够 45 分钟静默）→ `/stype extract` → 事件立刻出现，不用干等。
5. **归档事件仍可查**：面板把一件旧事件归档 → 问「那次 / 当时…」（history 路由）→ 仍能带出；日常问题不再带出。
6. **改名跟随到底**：档案里改某人昵称 → 点开含他的事件，参与人显示新名；事实卡「相关」里的人名实体也更新。
7. **查别人的过去**：群里问「小明以前喜欢什么」→ 能带出小明的旧说法（带「现在：」对照）；陌生人的私聊旧事依然不出现。
8. **已提过的不重复**：问「我以前喜欢美式吗」→ `【当时】` 块不应再出现美式那条。
9. **模型挂了事件不断**：把整理模型临时配错（或断网）→ 聊一段 → 事件页应出现「待审事件」（兜底摘要）而不是什么都没有；
   恢复配置后 `/stype extract` 正常。事实侧会退回启发式（标未审核），这是已知取舍。
10. 离线已覆盖的不用实机重测：脏数字/畸形 JSON（`test_soak.py` 毒丸用例）、并发写入、几千条规模、老库迁移。

## 3. 上一版（v4.2.0：事件层 + 隐私隔离 + 约定分槽）快速过一遍

1. 聊一段有内容的天（≥4 条用户消息，或出现「去了/决定/参加」）→ 静默 45 分钟后自动整理成事件；
   `/stype events` 或面板「事件」页可见，点「看原文」能看到那段消息。
2. 同一话题停 1 小时又接着聊 → 不产生第二条，原事件被更新。
3. 故意让摘要跑偏 → 进「待审事件」，点「确认写入」后才参与注入。
4. 问「最近怎么样」→ 包里出现 `【事件】`；同窗口紧接着再问不重复注入。
5. 私聊的私事在群里点名也不注入；主人记忆只在主人会话注入（切 `off` 对比）。
6. `答应小明带饭` + `答应周五交作业` → 两条并存；`做完了` 只归档最近那条。

## 4. 历史未确认项

1. 同词不同义分槽：`我喜欢喝美式。` + `我喜欢美式穿搭。` → 两条，领域 饮品/穿搭。
2. 安全阀：无语境句 + 带语境句 → 待确认；通过=存两条，驳回=丢新条。
3. 主人主语/列举偏好：`主人喜欢喝咖啡，打篮球。` → 拆两条；`主人喜欢猫和狗。` → 两条；`和平精英和王者荣耀` 不拆坏。
4. 手动补记默认写主人，不进 admin 档案。
5. 动态颜色：开启后点「保存设置」不会被关掉；换主题后左侧导航高亮色跟随。
6. Savage 记忆可写入；`以后你叫小美` 成为 Bot 的 name 记忆（仅主人有效）。
7. 选好图片转述模型发图 → `[图片] 转述文字` + `image_caption_ok`。
8. 重复问题第二次 `cache=hit`；慢接口出现 `provider_timeout` 并回退 `path=basic`。

## 5. 离线回归（不用上机）

```text
python tests/test_core.py -v        # 219 passed
python tests/test_soak.py -v        # 14 passed
```

有 astrbot / jsdom 时（见 README 第七节）：`tests/test_integration.py` 39 passed、
`tests/test_panel.mjs` 17 passed、`tests/verify_readme.py` ALL CHECKS PASSED。

## 判定

失败时发我：聊天原文 + `/stype status` + 诊断页 JSON + 带 `Savage Type` 的日志。
