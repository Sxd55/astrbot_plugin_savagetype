# Savage Type 实机测试流程（v4.1.0）

只列**本轮新增 + 历史未确认项**。

## 0. 升级

1. 覆盖目录 → 重载插件；`/stype status` 第一行应为 `v4.1.0`。
2. 检索行应为 `bm25 开(jieba)`；显示 `builtin` 表示 jieba 没装上（功能仍可用，只是字符切分）。
3. 版本变化会自动备份一次，诊断页可见。

## 1. 本轮新增（v4.1.0）

1. **设置页新布局**：左侧导航切换分组，右侧只显示选中组；改一个值不保存 → 切组再回来值还在；保存后刷新仍生效；窗口拉窄到 940px 以下导航变顶部横向条；「外观」在导航里。
2. **待确认覆盖面板**：制造一次冲突（高证据旧条被新说法挑战）→ 面板显示「旧 → 新」两行对比、说话人、来源、时间、原因中文说明；按钮写清「通过（新覆盖旧）」或「通过（存成两条）」；确认结果与说明一致。
3. **作废保留 + 回滚**：说「我改口了，不喜欢X了」→ `superseded` +1；回收站出现「已覆盖」旧条；存活事实卡出现「回滚改口」按钮，点击后旧条恢复 live、新条进回收站。
4. **学习审查节流**：聊 10 分钟以上 → few-shot 待审不再刷屏（默认 10 分钟扫一次、每轮最多 5 条，`/stype learn` 可强制立即扫）；面板支持状态/类型筛选、勾选批量批准/驳回、已审改回待审。
5. **黑话统计范围**：默认 `all`（群友的词也进统计）；把 `jargon_scope` 切 `owner` 后只统计主人。
6. **BM25 检索**：`/stype explain <关键词>` 看命中；设置里 `retrieval_bm25` 关掉可回退旧打分。

## 2. 历史未确认项（快速过一遍）

1. 同词不同义分槽：`我喜欢喝美式。` + `我喜欢美式穿搭。` → 两条，领域 饮品/穿搭。
2. 安全阀：无语境句 + 带语境句 → 待确认；通过=存两条，驳回=丢新条。
3. 主人主语/列举偏好：`主人喜欢喝咖啡，打篮球。` → 拆两条；`主人喜欢猫和狗。` → 两条；`和平精英和王者荣耀` 不拆坏。
4. 手动补记默认写主人，不进 admin 档案。
5. 动态颜色：开启后点「保存设置」不会被关掉；换主题后左侧导航高亮色跟随。
6. Savage 记忆可写入；`以后你叫小美` 成为 Bot 的 name 记忆（仅主人有效）。
7. 选好图片转述模型发图 → `[图片] 转述文字` + `image_caption_ok`。
8. 重复问题第二次 `cache=hit`；慢接口出现 `provider_timeout` 并回退 `path=basic`。

## 判定

失败时发我：聊天原文 + `/stype status` + 诊断页 JSON + 带 `Savage Type` 的日志。
