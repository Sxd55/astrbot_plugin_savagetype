# Savage Type 实机测试流程（v3.2.0）

离线测试只验证库和规则。按顺序走一遍，确认 AstrBot 上的采集、管线、注入、生命周期、回收站都正常。

## 0. 安装 / 升级

1. 覆盖整个 `astrbot_plugin_savagetype` 目录（名字不要改），重载插件。首次加载会看到日志：
   - `clean rebuild done`（仅 v2.8 升级那次）
   - `version backup: 3.2.0 -> .../backups/...`（本次升级会多一份备份）
2. 打开拓展页，确认：
   - 顶栏是着色器渐变风格，背景有缓慢流动的极光；
   - 右上角 Logo 正常显示（`logo.png`）；
   - 设置 → 外观有 5 组预设，默认「极光」。

## 1. 基础状态

对 Bot 发 `/stype status`，第一行应是 `v3.2.0`，并能看到时间线、主人记忆、档案、待审记忆、允许平台、上次采集跳过。

## 2. 采集与建档

1. 非主人在 QQ 群发言 → 人物档案应自动出现该人。
2. ChatUI 里发一句 `我喜欢喝美式，不喜欢拿铁。` → 时间线增加，档案不增加新面孔。

## 3. AI 管线

等 45 秒或面板点「抽取/整理」。记忆库应出现主人记忆，标签为「AI 通过」，可展开原文。

## 4. 注入

问 `我喜欢喝什么来着？` Bot 应能答出。诊断页「注入显微镜」有对应快照（route / core / chars）。

## 5. 空闲触发抽取（新增）

1. 先确认「抽取阈值」是 8，只发 1 条事实。
2. 静默 5 分钟以上（默认空闲 300 秒），再随便发一条消息。
3. `/stype status` 的「未总结」应清零，记忆库出现那条事实（说明空闲触发生效）。

## 6. 重要性 + 置顶 + 归档（新增）

1. 记忆库卡片应显示「权重」徽章（实时分）。点「编辑」可改基础重要度（0–1），保存后权重随之变化。
2. 给一条记忆点「置顶」→ 出现「置顶」徽章；再问相关问题时它更优先。
3. 把「维护」点一次：低价值、长期未访问的记忆会归档；`/stype status` 的 live 数量可能下降。
4. 诊断页「回收站」能看到被归档的记忆，点「恢复」应回到 live。
5. 若槽位已被新记忆占用，恢复会被阻止（诊断 JSON 里 blocked 有冲突 id），先删/归档新的再恢复。

## 6.5 线索词检索（新增）

1. 造一条带线索词的记忆（例如说「记住，我咨询过境外支付，用的万事达卡」，整理后可看到关键词）。
2. `/stype search 万事达` 应能命中那条（即使正文里没有「万事达」三个字也没关系）。

## 7. 跨轮注入去重（新增）

1. 连问两次同一句 `我喜欢喝什么？`（间隔几秒）。
2. 第二次「注入显微镜」里 `core` 应减少或为空，诊断 JSON 的 `dedup` 大于 0，blocked 里能看到 `recently_injected`。
3. 再说 `你还记得我喜欢什么吗？` → 去重豁免，记忆应再次注入（route=recall）。

## 8. 约定 / 近况分块（新增）

1. 说 `记住，我下周三要去体检。` 和 `我这周在加班。`
2. 整理后问一句相关的问题，注入显微镜对应的包里应出现 `【约定】` 和 `【近况】` 两个块（可在诊断 JSON 或日志里确认）。

## 9. 图片转述（可选）

1. 设置里给「图片转述模型」选一个视觉模型（留空则完全不做额外调用）。
2. 在 QQ 或 ChatUI 发一张图片。
3. `/stype recent 3` 应出现 `[图片] ...` 的时间线记录。

## 10. 回收站与恢复（新增）

同第 6 步的第 3/4 条；恢复后在记忆库能看到原条目，且不会和现存的同槽记忆打架。

## 11. 版本备份（新增）

升级后看数据目录 `backups/`，应有一份本次版本号命名的备份；`/stype status` 正常说明没有把库改坏。

## 12. 旧能力回归

学习审查（黑话/few-shot）批准后注入；导出 JSONL；聊天导入预览/确认；说话人归并建议。都应正常。

## 判定

- 0–12 基本通过即可收口；第 9 步按需。
- 失败时把以下内容发我：该步的聊天原文、`/stype status`、`/stype microscope`、诊断页 JSON、AstrBot 日志里带 `Savage Type` 的行。
