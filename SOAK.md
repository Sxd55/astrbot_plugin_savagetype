# Savage Type 实机测试流程（v4.0.0）

只列**本轮修复 + 未确认项**。

## 0. 升级

1. 覆盖目录 → 重载插件；`/stype status` 第一行应为 `v4.0.0`。

## 1. 列举偏好 + 主人主语（本轮修复）

上轮失败原因：`主人喜欢…` 不被识别为偏好（只认「我」），全部落 note 单槽互相覆盖；且「喜欢咖啡，打篮球」只记第一项。

1. 依次发：
   - `主人喜欢喝咖啡，打篮球。`
   - `主人喜欢打篮球。`
2. 期望：第一条拆成**两条** likes —— 咖啡（领域 饮品）+ 打篮球（领域 运动）；第二条只刷新「打篮球」，**咖啡还在**。
3. 聊天发和面板「主人手动补记」两条路径都要验证。
4. 顺带：
   - `主人喜欢猫和狗。` → 两条（猫/狗）；
   - `主人喜欢和平精英和王者荣耀。` → 两条，且「和平精英」不被拆坏（本轮修复：AI 缩写后不带主语也能拆）；
   - `主人住在北京和上海。` → 一条 identity，不拆。
5. **无主语句**（上一版漏掉的情况）：聊天里 AI 整理出的「喜欢和平精英和王者荣耀」这类不带「我/主人」的结果，也要拆成两条。

## 2. 未确认的历史项（快速过一遍）

1. 同词不同义分槽：`我喜欢喝美式。` + `我喜欢美式穿搭。` → 两条，领域 饮品/穿搭。
2. 安全阀：无语境句 + 带语境句 → 待确认；通过=存两条，驳回=丢新条。
3. 手动补记默认写主人，不进 admin 档案。
4. 开启动态颜色后点「保存设置」不会被关掉；开着动态时刷新/操作不闪回基础色。
5. Savage 记忆可写入；`以后你叫小美` 成为 Bot 的 name 记忆（仅主人有效）。
6. 设置页 8 组默认收起；保存后刷新仍是新值。
7. 选好图片转述模型发图 → `[图片] 转述文字` + `image_caption_ok`。
8. 重复问题第二次 `cache=hit`；慢接口出现 `provider_timeout` 并回退 `path=basic`。

## 判定

失败时发我：聊天原文 + `/stype status` + 诊断页 JSON + 带 `Savage Type` 的日志。
